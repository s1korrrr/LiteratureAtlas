[package]
name = "atlas_graph"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
polars = { version = "0.40", features = ["parquet", "lazy", "fmt"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
